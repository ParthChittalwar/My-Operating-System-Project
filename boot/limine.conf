# NovaOS Limine Bootloader Configuration (Limine 8.x format)

# The timeout in seconds before the default entry boots
timeout: 3

# Entry name
/NovaOS
    protocol: limine
    kernel_path: boot():/boot/kernel.elf
